import Blog from "@/components/blogs/Blog";
import React from "react";
export const metadata = {
  title: "Blog",
};
const page = () => {
  return (
    <div>
      <Blog />
    </div>
  );
};

export default page;
